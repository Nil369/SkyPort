---
title: Project Structure
navigation:
  icon: i-lucide-folder-tree
description: Understand the SkyPort repository layout and runtime boundaries.
---

SkyPort keeps the repository split into a small number of clearly defined surfaces.

::code-tree{default-value="package.json"}

```json [package.json]
{
  "name": "skyport",
  "version": "1.0.0",
  "description": "Self-hosted infrastructure platform",
  "private": true
}
```


```markdown [README.md]
# SkyPort

Self-hosted infrastructure platform for developers and operators.

- Docker management
- PM2 process control
- VPS operations
- Reverse proxy workflows
- Terminal-first automation
```

```go [backend/cmd/server/main.go]
package main

func main() {
    // Backend server entry point
}
```

```go [cli/main.go]
package main

func main() {
    // CLI entrypoint
}
```

```ts [frontend/src/main.tsx]
import React from 'react'
import ReactDOM from 'react-dom/client'
import App from './App'
```

```bash [installer/install.sh]
#!/usr/bin/env bash
set -Eeuo pipefail

# SkyPort installation script
```

```ts [docs/nuxt.config.ts]
// Docus documentation configuration
export default defineNuxtConfig({
  extends: ['docus']
})
```

::

## Backend

The backend owns API routes, WebSocket streams, runtime collection, and host integration.

::code-tree{default-value="backend/cmd/server/main.go"}

```go [backend/go.mod]
module skyport

go 1.25.0

require (
	github.com/charmbracelet/lipgloss v1.1.0
	github.com/creack/pty v1.1.24
	github.com/glebarez/sqlite v1.11.0
	github.com/go-playground/validator/v10 v10.30.2
	github.com/gofiber/fiber/v2 v2.52.5
	github.com/gofiber/websocket/v2 v2.2.1
	github.com/golang-jwt/jwt/v5 v5.3.1
	github.com/google/uuid v1.6.0
	github.com/joho/godotenv v1.5.1
	github.com/shirou/gopsutil/v3 v3.24.5
)
```

```gitignore [backend/.gitignore]
# Build artifacts
/bin/
/dist/
*.exe
*.test

# Local data (SQLite)
/data/
*.db
*.db-shm
*.db-wal

# Env files
.env
/skyport/workspace
```

```makefile [backend/Makefile]
.PHONY: build dev test

build:
	go build -o skyport-server ./cmd/server

dev:
	go run ./cmd/server

test:
	go test ./...
```

```markdown [backend/README.md]
# SkyPort Backend

Go backend server and API implementation.

Modules:
- api/       → HTTP route handlers
- docker/    → Docker client
- metrics/   → Metrics collection
- marketplace/ → Marketplace service
- pm2/       → Process manager
- proxy/     → Reverse proxy
```

```bash [backend/data/.gitkeep]
# Placeholder for data directory
```

```go [backend/cmd/server/main.go]
package main

func main() {
    // Backend server entry point
}
```

```go [backend/internal/api/handler.go]
package api

// API route handlers
```

```go [backend/internal/docker/client.go]
package docker

// Docker client implementation
```

```go [backend/internal/metrics/collector.go]
package metrics

// Metrics collection
```

```go [backend/internal/marketplace/service.go]
package marketplace

// Marketplace service
```

```go [backend/internal/pm2/manager.go]
package pm2

// PM2 process manager
```

```go [backend/internal/proxy/reverse.go]
package proxy

// Reverse proxy implementation
```

::

## CLI

The CLI provides the operator-facing command surface.

::code-tree{default-value="cli/main.go"}

```go [cli/go.mod]
module skyport-cli

go 1.23.0

toolchain go1.24.2

require (
	github.com/charmbracelet/bubbletea v1.3.4
	github.com/charmbracelet/lipgloss v1.1.0
	github.com/gorilla/websocket v1.5.3
	github.com/pterm/pterm v0.12.80
	github.com/spf13/cobra v1.8.1
	github.com/spf13/viper v1.20.1
	github.com/zalando/go-keyring v0.0.0
	golang.org/x/crypto v0.32.0
)
```

```go [cli/main.go]
package main

import (
	"log"

	"skyport-cli/cmd"
)

func main() {
	if err := cmd.Execute(); err != nil {
		log.Fatal(err)
	}
}
```

```makefile [cli/Makefile]
.PHONY: build dev test

build:
	go build -o skyport ./main.go

dev:
	go run ./main.go

test:
	go test ./...
```

```yaml [cli/configs/default.yaml]
# Default CLI configuration
host: localhost
port: 8080
environment: development
```

```go [cli/cmd/root.go]
package cmd

// Root command
```

```go [cli/internal/api/client.go]
package api

// API client for backend communication
```

```go [cli/internal/config/config.go]
package config

// Configuration management
```

```go [cli/internal/docker/docker.go]
package docker

// Docker CLI operations
```

```go [cli/internal/tui/ui.go]
package tui

// Terminal user interface
```

::

## Runtime Storage

The CLI stores its local state under `~/.skyport/`.

::code-tree{default-value="~/.skyport/config.yaml"}

```yaml [~/.skyport/config.yaml]
# SkyPort CLI configuration
host: localhost
port: 8080
token: ""
authenticated: false
```

```bash [~/.skyport/.gitignore]
# Local cache and temporary files
cache/
tmp/

# Sensitive data
*.env.local
*.key
token

# Logs
logs/*.log
```

```json [~/.skyport/state.json]
{
  "lastHost": "localhost",
  "lastPort": 8080,
  "lastProject": null,
  "recentProjects": [],
  "preferences": {
    "theme": "auto",
    "verbosity": "info"
  }
}
```

```markdown [~/.skyport/README.md]
# SkyPort CLI Local Storage

This directory stores your local SkyPort CLI configuration and state.

## Directories

- **data/** → Persistent CLI data
- **workspace/** → Project workspace files
- **logs/** → CLI execution logs
- **cache/** → Cached API responses
- **tmp/** → Temporary work files

## Files

- **config.yaml** → Main configuration (host, port, auth token)
- **state.json** → CLI state and preferences
- **.gitignore** → Local ignore rules
```

::

## Build Targets

- `make backend-build` builds the backend server.
- `make cli-build` builds the CLI binary.
- `make build-all` builds the embedded UI plus release binaries.
